<Window x:Class="DrawClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:views="clr-namespace:DrawClient.Views"
        xmlns:usercontrols="clr-namespace:DrawClient.Views.UserControls"
        xmlns:viewmodels="clr-namespace:DrawClient.ViewModels"
        
        mc:Ignorable="d"
        Title="CollabDraw" Height="800" Width="1200" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <views:LoginScreen />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:LobbyViewModel}">
            <views:Dashboard />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:WhiteboardViewModel}">
            <usercontrols:Canvas />
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding CurrentView}" />

</Window>